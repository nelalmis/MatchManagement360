import {
    StyleSheet
} from 'react-native';

const globalStyles = StyleSheet.create({
    pageHeader: {
        backgroundColor: '#16a34a',
        padding: 16,
        flexDirection: 'row',
        alignItems: 'center',
        marginTop: 30
    },
    headerButton: {
        marginRight: 16,
    },
    headerTitle: {
        fontSize: 20,
        fontWeight: 'bold',
        color: '#fff',
    },
});

export default globalStyles;